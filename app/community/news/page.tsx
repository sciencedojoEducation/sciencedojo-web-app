import type { Metadata } from "next";
/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link";
import { ExternalLink } from "lucide-react";
import { getPublishedExamUpdates } from "@/lib/community";
import { siteUrl } from "@/lib/seo";
export const metadata: Metadata = { title: "Official GCSE & A-Level Exam Updates | ScienceDojo", description: "Dated, sourced summaries of official UK exam-board, Ofqual, JCQ, GOV.UK, and UCAS updates.", alternates: { canonical: `${siteUrl}/community/news` } };
export default async function NewsPage() { const updates = await getPublishedExamUpdates(50); return <main className="min-h-screen bg-[#f7fbff] px-4 py-12 text-secondary"><div className="mx-auto max-w-4xl"><Link href="/community" className="text-sm font-black text-primary">← Exam Community</Link><p className="mt-7 text-xs font-black uppercase tracking-widest text-primary">Primary sources only</p><h1 className="mt-3 text-4xl font-black">Official exam updates</h1><p className="mt-4 max-w-2xl font-semibold leading-7 text-secondary/55">Short editorial summaries with a direct source and publication date. Always confirm arrangements with your school or college.</p><div className="mt-8 space-y-4">{updates.map((update: any) => <article key={update.id} className="rounded-3xl border border-secondary/10 bg-white p-6"><div className="flex flex-wrap gap-2 text-[10px] font-black uppercase tracking-widest text-primary"><span>{update.source_name}</span><time className="text-secondary/35">{new Date(update.source_published_at).toLocaleDateString("en-GB", { day: "numeric", month: "long", year: "numeric" })}</time></div><h2 className="mt-3 text-2xl font-black">{update.title}</h2><p className="mt-3 font-semibold leading-7 text-secondary/60">{update.summary}</p>{update.editorial_note && <p className="mt-4 rounded-2xl bg-primary/5 p-4 text-sm font-semibold leading-6"><strong>ScienceDojo note:</strong> {update.editorial_note}</p>}<a href={update.source_url} target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 text-sm font-black text-primary">Read the official source <ExternalLink size={14}/></a></article>)}{updates.length === 0 && <div className="rounded-3xl border border-dashed border-secondary/15 bg-white p-10 text-center font-bold text-secondary/40">No official updates have been published yet.</div>}</div></div></main>; }
